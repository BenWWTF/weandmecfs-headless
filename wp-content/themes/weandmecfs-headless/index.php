<?php
/**
 * Index template — never rendered in production. The front-end is
 * served by the Next.js process; this file exists only so WordPress
 * sees a valid theme.
 *
 * The 410 + redirect happens in functions.php's `template_redirect`
 * hook, so by the time this file is reached the response is already
 * sent. Keep the body empty.
 */
